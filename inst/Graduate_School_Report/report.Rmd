---
title: "Graduate School Report"
author: "Admissions and Recruiting"
output:
    pdf_document:
        includes:
            in_header: mystyles.sty
---

```{r, include = FALSE}
knitr::opts_chunk$set(echo = FALSE)
options(xtable.comment = FALSE)
```

```{r, include = FALSE}
inFile <- input$file
browser()
inFile <- inFile[!(str_detect(inFile$name, ".docx")),]
file.rename(inFile$datapath,
            paste(inFile$datapath, ".xls", sep=""))
files <- paste(inFile$datapath, ".xls", sep="") %>% read_clean
summary <- files$crystal %>% application_summary
```

```{r, child = 'title.Rmd', eval = 1 == 1}
```

```{r, include = FALSE}
files$crystal %<>% 
  filter(DEGR %in% input$degrees) %>%
  filter(MAJOR %in% input$majors) %>% 
  gather("Variable", "Value", 16:26)
```
## Degree: `r files$crystal$DEGR %>% unique`
## Major: `r files$crystal$MAJOR %>% unique`

\newpage
```{r, child = 'applications.Rmd', eval = 1 == 1}
```

```{r, child = 'offerRejectionCancelled.Rmd', eval = 1 == 1}
```

```{r, child = 'acceptedDeclined.Rmd', eval = 1 == 1}
```
